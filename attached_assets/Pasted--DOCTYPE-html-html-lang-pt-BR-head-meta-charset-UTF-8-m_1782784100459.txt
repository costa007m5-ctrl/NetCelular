<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
  <title>Netplay Loading V24</title>

  <style>
    :root{
      --cycle: 6.8s;
      --red:#ff1f33;
      --red-soft:#ff5b68;
      --red-dark:#8f0008;
      --white:#f6f6f8;
      --soft:rgba(255,255,255,.28);
      --sub:rgba(214,194,198,.42);
      --track:rgba(255,255,255,.08);
    }

    *{box-sizing:border-box;margin:0;padding:0}

    html,body{
      width:100%;
      height:100%;
      overflow:hidden;
      background:#000;
      font-family:Inter,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",Arial,sans-serif;
      color:#fff;
    }

    body{display:grid;place-items:center}

    .loading-screen{
      position:relative;
      width:100vw;
      height:100vh;
      min-height:100svh;
      overflow:hidden;
      isolation:isolate;
      display:flex;
      align-items:center;
      justify-content:center;
      background:
        radial-gradient(circle at 50% 34%, rgba(255,27,42,.10), transparent 22%),
        linear-gradient(180deg, #090002 0%, #040001 56%, #000 100%);
    }

    .loading-screen::before{
      content:"";
      position:absolute;
      inset:0;
      z-index:6;
      pointer-events:none;
      background:
        radial-gradient(circle at center, transparent 0 46%, rgba(0,0,0,.46) 82%, rgba(0,0,0,.86) 100%),
        linear-gradient(180deg, rgba(255,255,255,.03), transparent 22%, transparent 78%, rgba(0,0,0,.22));
    }

    .loading-screen::after{
      content:"";
      position:absolute;
      inset:0;
      z-index:7;
      pointer-events:none;
      opacity:.13;
      background:
        repeating-linear-gradient(
          180deg,
          rgba(255,255,255,.018) 0px,
          rgba(255,255,255,.018) 1px,
          transparent 1px,
          transparent 8px
        );
      animation:scan 11s linear infinite;
    }

    .bg{
      position:absolute;
      inset:0;
      display:grid;
      place-items:center;
      pointer-events:none;
      z-index:1;
    }

    .orb{
      position:absolute;
      width:min(62vw, 420px);
      height:min(62vw, 420px);
      border-radius:50%;
      background:radial-gradient(circle, rgba(255,25,40,.18), rgba(255,25,40,.08) 40%, rgba(255,25,40,0) 72%);
      filter:blur(8px);
      animation:orbPulse 4s ease-in-out infinite;
    }

    .ring{
      position:absolute;
      border-radius:50%;
      border:1px solid rgba(255,24,42,.11);
      background:rgba(255,0,0,.02);
      animation:ringPulse 4.8s ease-in-out infinite;
    }

    .ring.r1{width:min(86vw, 620px);height:min(86vw, 620px)}
    .ring.r2{width:min(64vw, 470px);height:min(64vw, 470px);animation-delay:.2s}
    .ring.r3{width:min(44vw, 320px);height:min(44vw, 320px);animation-delay:.45s}

    .particles{
      position:absolute;
      inset:0;
      pointer-events:none;
      z-index:2;
    }

    .particle{
      position:absolute;
      width:4px;
      height:4px;
      border-radius:50%;
      background:rgba(255,105,115,.85);
      box-shadow:0 0 10px rgba(255,70,85,.38);
      opacity:0;
      animation:particleFloat 7s ease-in-out infinite;
    }

    .particle:nth-child(1){left:17%;top:29%;animation-delay:.3s}
    .particle:nth-child(2){left:27%;top:68%;animation-delay:1.1s}
    .particle:nth-child(3){left:42%;top:22%;animation-delay:.7s}
    .particle:nth-child(4){left:67%;top:25%;animation-delay:1.3s}
    .particle:nth-child(5){left:81%;top:65%;animation-delay:.9s}
    .particle:nth-child(6){left:58%;top:73%;animation-delay:1.6s}

    .content{
      position:relative;
      z-index:4;
      width:min(84vw, 520px);
      display:flex;
      flex-direction:column;
      align-items:center;
      text-align:center;
      transform:translateY(-1.8vh);
      animation:contentCycle var(--cycle) cubic-bezier(.2,.9,.2,1) infinite;
    }

    .logo-wrap{
      position:relative;
      width:clamp(132px, 28vw, 180px);
      height:clamp(132px, 28vw, 180px);
      display:grid;
      place-items:center;
      margin-bottom:28px;
      animation:logoOpenClose var(--cycle) cubic-bezier(.2,.9,.2,1) infinite;
    }

    .logo-glow{
      position:absolute;
      inset:-20%;
      border-radius:50%;
      background:radial-gradient(circle, rgba(255,45,60,.42) 0%, rgba(255,45,60,.16) 42%, rgba(255,45,60,0) 74%);
      filter:blur(16px);
      animation:glowPulse 2.7s ease-in-out infinite;
    }

    .logo{
      position:relative;
      width:100%;
      height:100%;
      border-radius:28%;
      overflow:hidden;
      background:linear-gradient(180deg, #ff4954 0%, #f40309 45%, #970008 100%);
      box-shadow:
        0 18px 34px rgba(0,0,0,.34),
        0 0 40px rgba(255,25,35,.16),
        inset 0 2px 0 rgba(255,255,255,.24),
        inset 0 -2px 0 rgba(0,0,0,.22);
      animation:logoFloat 3.6s ease-in-out infinite;
    }

    .logo::before{
      content:"";
      position:absolute;
      left:0; right:0; top:0;
      height:52%;
      background:linear-gradient(180deg, rgba(255,255,255,.16), rgba(255,255,255,0));
      pointer-events:none;
    }

    .logo::after{
      content:"";
      position:absolute;
      inset:0;
      background:linear-gradient(90deg, transparent, rgba(255,255,255,.14), transparent);
      transform:translateX(-145%) skewX(-18deg);
      animation:logoShine 4.2s ease-in-out infinite;
      pointer-events:none;
    }

    .mark{
      position:absolute;
      inset:0;
      display:grid;
      place-items:center;
    }

    .n-stage{
      position:relative;
      width:54%;
      height:58%;
      display:grid;
      place-items:center;
    }

    .n-back{
      position:absolute;
      inset:0;
      display:grid;
      place-items:center;
      font-family:"Arial Black", Impact, Inter, Arial, sans-serif;
      font-size:clamp(80px, 16vw, 112px);
      line-height:1;
      font-weight:900;
      color:#f7f7f8;
      opacity:.16;
      filter:blur(8px);
      transform:translateY(-2px) scale(1.08);
      animation:nAura var(--cycle) ease-in-out infinite;
    }

    .n-letter{
      position:absolute;
      inset:0;
      display:grid;
      place-items:center;
      font-family:"Arial Black", Impact, Inter, Arial, sans-serif;
      font-size:clamp(80px, 16vw, 112px);
      line-height:1;
      font-weight:900;
      color:#f7f7f8;
      text-shadow:0 2px 8px rgba(0,0,0,.10);
      letter-spacing:-0.04em;
      transform:translateY(-3px);
      opacity:0;
    }

    .n-left{
      clip-path:inset(0 50% 0 0);
      animation:nLeftBuild var(--cycle) cubic-bezier(.2,.9,.2,1) infinite;
    }

    .n-right{
      clip-path:inset(0 0 0 50%);
      animation:nRightBuild var(--cycle) cubic-bezier(.2,.9,.2,1) infinite;
    }

    .n-shine{
      position:absolute;
      inset:-8% 22%;
      background:linear-gradient(90deg, transparent, rgba(255,255,255,.56), transparent);
      transform:translateX(-170%) skewX(-18deg);
      filter:blur(1px);
      mix-blend-mode:screen;
      animation:nShinePass var(--cycle) ease-in-out infinite;
      pointer-events:none;
    }

    .play-badge{
      position:absolute;
      right:11%;
      bottom:11%;
      width:31%;
      aspect-ratio:1/1;
      border-radius:50%;
      background:#f5f5f7;
      display:grid;
      place-items:center;
      box-shadow:
        0 7px 16px rgba(0,0,0,.22),
        inset 0 1px 0 rgba(255,255,255,.62);
      animation:playPulse 2.2s ease-in-out infinite;
    }

    .play-badge::before{
      content:"";
      width:0;
      height:0;
      border-top:10px solid transparent;
      border-bottom:10px solid transparent;
      border-left:15px solid #df1e2d;
      margin-left:4px;
    }

    .brand{
      line-height:.92;
      font-weight:900;
      font-size:clamp(48px, 11vw, 80px);
      letter-spacing:.08em;
      text-shadow:0 10px 22px rgba(0,0,0,.33);
      display:flex;
      align-items:center;
      justify-content:center;
      flex-wrap:wrap;
      gap:.03em;
    }

    .letters{
      display:flex;
      align-items:center;
      gap:.03em;
    }

    .letter{
      display:inline-block;
      opacity:0;
      transform:translateY(18px) scale(.96);
      animation:letterCycle var(--cycle) cubic-bezier(.2,.9,.2,1) infinite;
    }

    .letter.net{ color:var(--red); }
    .letter.play{ color:var(--white); }

    .letter:nth-child(1){ animation-delay:.85s; }
    .letter:nth-child(2){ animation-delay:.95s; }
    .letter:nth-child(3){ animation-delay:1.05s; }
    .letter:nth-child(4){ animation-delay:1.18s; }
    .letter:nth-child(5){ animation-delay:1.28s; }
    .letter:nth-child(6){ animation-delay:1.38s; }
    .letter:nth-child(7){ animation-delay:1.48s; }

    .brand-glow{
      position:absolute;
      margin-top:8px;
      width:min(72vw, 420px);
      height:70px;
      border-radius:50%;
      background:radial-gradient(circle, rgba(255,34,50,.32), transparent 70%);
      filter:blur(18px);
      opacity:0;
      animation:brandGlow var(--cycle) ease-in-out infinite;
      pointer-events:none;
    }

    .subtitle{
      margin-top:20px;
      color:var(--text-sub);
      font-size:clamp(11px, 2vw, 15px);
      font-weight:700;
      letter-spacing:.32em;
      text-transform:uppercase;
      white-space:nowrap;
      opacity:0;
      animation:subtitleCycle var(--cycle) ease-in-out infinite;
    }

    .loader{
      width:min(72vw, 440px);
      margin-top:86px;
      opacity:0;
      animation:loaderCycle var(--cycle) ease-in-out infinite;
    }

    .progress{
      position:relative;
      width:100%;
      height:8px;
      border-radius:999px;
      overflow:hidden;
      background:var(--track);
      box-shadow: inset 0 1px 0 rgba(255,255,255,.04), inset 0 -1px 0 rgba(0,0,0,.25);
    }

    .fill{
      position:absolute;
      left:0;
      top:0;
      bottom:0;
      width:0%;
      border-radius:inherit;
      background:linear-gradient(90deg, #ff1125 0%, #ff3345 44%, #ff7881 76%, #ff2335 100%);
      box-shadow:0 0 16px rgba(255,40,52,.22);
      animation:loadingBar var(--cycle) ease-in-out infinite;
    }

    .fill::after{
      content:"";
      position:absolute;
      top:0;
      bottom:0;
      right:-34px;
      width:84px;
      background:linear-gradient(90deg, transparent, rgba(255,255,255,.34), transparent);
      transform:skewX(-18deg);
      filter:blur(.6px);
    }

    .loading-text{
      margin-top:26px;
      color:var(--soft);
      font-size:clamp(16px, 2.7vw, 22px);
      font-weight:600;
      line-height:1.35;
      animation:textFade 2s ease-in-out infinite;
    }

    .dots::after{
      content:"";
      animation:dots 1.4s steps(4,end) infinite;
    }

    .blackout{
      position:absolute;
      inset:0;
      background:#000;
      opacity:0;
      pointer-events:none;
      z-index:8;
      animation:blackoutCycle var(--cycle) ease-in-out infinite;
    }

    @keyframes contentCycle{
      0%{opacity:0; transform:translateY(16px) scale(.985)}
      10%,84%{opacity:1; transform:translateY(-1.8vh) scale(1)}
      100%{opacity:0; transform:translateY(-10px) scale(1.02)}
    }

    @keyframes logoOpenClose{
      0%{opacity:0; transform:translateY(18px) scale(.80)}
      13%{opacity:1; transform:translateY(0) scale(1.04)}
      22%,82%{opacity:1; transform:translateY(0) scale(1)}
      92%{opacity:.9; transform:translateY(-6px) scale(1.05)}
      100%{opacity:0; transform:translateY(-16px) scale(1.10)}
    }

    @keyframes nLeftBuild{
      0%,8%{opacity:0; transform:translate(-16px, 8px) scale(.86)}
      18%{opacity:1; transform:translate(4px, 0) scale(1.02)}
      25%,82%{opacity:1; transform:translate(0, 0) scale(1)}
      100%{opacity:0; transform:translate(-10px, -6px) scale(1.03)}
    }

    @keyframes nRightBuild{
      0%,10%{opacity:0; transform:translate(16px, -8px) scale(.86)}
      20%{opacity:1; transform:translate(-4px, 0) scale(1.02)}
      27%,82%{opacity:1; transform:translate(0, 0) scale(1)}
      100%{opacity:0; transform:translate(10px, -6px) scale(1.03)}
    }

    @keyframes nAura{
      0%,10%{opacity:0; transform:translateY(-2px) scale(.86)}
      22%,78%{opacity:.16; transform:translateY(-2px) scale(1.08)}
      100%{opacity:0; transform:translateY(-2px) scale(1.16)}
    }

    @keyframes nShinePass{
      0%,24%{opacity:0; transform:translateX(-170%) skewX(-18deg)}
      32%{opacity:.9}
      44%,100%{opacity:0; transform:translateX(170%) skewX(-18deg)}
    }

    @keyframes letterCycle{
      0%,12%{opacity:0; transform:translateY(18px) scale(.96)}
      20%{opacity:1; transform:translateY(-3px) scale(1.03)}
      28%,82%{opacity:1; transform:translateY(0) scale(1)}
      100%{opacity:0; transform:translateY(-12px) scale(1.04)}
    }

    @keyframes brandGlow{
      0%,24%{opacity:0; transform:scale(.9)}
      36%,80%{opacity:1; transform:scale(1)}
      100%{opacity:0; transform:scale(1.08)}
    }

    @keyframes subtitleCycle{
      0%,22%{opacity:0; transform:translateY(10px)}
      34%,80%{opacity:1; transform:translateY(0)}
      100%{opacity:0; transform:translateY(-10px)}
    }

    @keyframes loaderCycle{
      0%,30%{opacity:0; transform:translateY(12px)}
      40%,84%{opacity:1; transform:translateY(0)}
      100%{opacity:0; transform:translateY(-8px)}
    }

    @keyframes blackoutCycle{
      0%,90%{opacity:0}
      100%{opacity:1}
    }

    @keyframes scan{
      from{transform:translateY(0)}
      to{transform:translateY(8px)}
    }

    @keyframes orbPulse{
      0%,100%{transform:scale(1);opacity:.75}
      50%{transform:scale(1.08);opacity:1}
    }

    @keyframes ringPulse{
      0%,100%{transform:scale(1);opacity:.8}
      50%{transform:scale(1.035);opacity:.55}
    }

    @keyframes particleFloat{
      0%,100%{opacity:0; transform:translateY(8px) scale(.7)}
      25%{opacity:.75}
      70%{opacity:.35}
      100%{opacity:0; transform:translateY(-20px) scale(1.06)}
    }

    @keyframes glowPulse{
      0%,100%{transform:scale(1);opacity:.78}
      50%{transform:scale(1.08);opacity:1}
    }

    @keyframes logoFloat{
      0%,100%{transform:translateY(0)}
      50%{transform:translateY(-6px)}
    }

    @keyframes logoShine{
      0%,58%{transform:translateX(-145%) skewX(-18deg)}
      73%,100%{transform:translateX(145%) skewX(-18deg)}
    }

    @keyframes playPulse{
      0%,100%{transform:scale(1)}
      50%{transform:scale(1.05)}
    }

    @keyframes loadingBar{
      0%,36%{width:0%}
      46%{width:12%}
      58%{width:36%}
      70%{width:62%}
      82%{width:88%}
      90%{width:100%}
      100%{width:100%}
    }

    @keyframes textFade{
      0%,100%{opacity:.55}
      50%{opacity:1}
    }

    @keyframes dots{
      0%{content:""}
      25%{content:"."}
      50%{content:".."}
      75%{content:"..."}
      100%{content:""}
    }

    @media (max-width:480px){
      .content{transform:translateY(-2vh)}
      .subtitle{
        width:92vw;
        white-space:normal;
        line-height:1.5;
        letter-spacing:.2em;
      }
      .loader{width:74vw; margin-top:74px}
      .loading-text{font-size:15px}
    }
  </style>
</head>
<body>
  <main class="loading-screen">
    <div class="bg" aria-hidden="true">
      <div class="orb"></div>
      <div class="ring r1"></div>
      <div class="ring r2"></div>
      <div class="ring r3"></div>
    </div>

    <div class="particles" aria-hidden="true">
      <span class="particle"></span>
      <span class="particle"></span>
      <span class="particle"></span>
      <span class="particle"></span>
      <span class="particle"></span>
      <span class="particle"></span>
    </div>

    <section class="content" aria-label="Tela de carregamento Netplay">
      <div class="logo-wrap">
        <div class="logo-glow"></div>
        <div class="logo">
          <div class="mark">
            <div class="n-stage">
              <div class="n-back">N</div>
              <div class="n-letter n-left">N</div>
              <div class="n-letter n-right">N</div>
              <div class="n-shine"></div>
            </div>
          </div>
          <div class="play-badge"></div>
        </div>
      </div>

      <div class="brand-glow" aria-hidden="true"></div>

      <div class="brand" aria-label="NETPLAY">
        <div class="letters">
          <span class="letter net">N</span>
          <span class="letter net">E</span>
          <span class="letter net">T</span>
          <span class="letter play">P</span>
          <span class="letter play">L</span>
          <span class="letter play">A</span>
          <span class="letter play">Y</span>
        </div>
      </div>

      <div class="subtitle">CATÁLOGO PREMIUM · ENTRETENIMENTO</div>

      <div class="loader">
        <div class="progress" aria-hidden="true">
          <div class="fill"></div>
        </div>
        <div class="loading-text">
          Carregando seu conteúdo personalizado<span class="dots"></span>
        </div>
      </div>
    </section>

    <div class="blackout" aria-hidden="true"></div>
  </main>
</body>
</html>